{
  "name": "@ukbt/truth",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "UKBT content schemas + truth gate. Owns no framework/UI code. See contracts/TRUTH-CONTRACT.md, contracts/CONTENT-CONTRACT.md.",
  "exports": {
    ".": "./src/index.ts",
    "./gate": "./src/gate/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "tokens:build": "style-dictionary build --config style-dictionary.config.json"
  },
  "dependencies": {
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^4.1.11",
    "style-dictionary": "^4.3.3"
  }
}
