# PROMPT 14 — FOUNDATION (Stage 4)

The repository contract is frozen. Implement the minimum production-ready
foundation. Do NOT attempt the complete website.

Implement only: 1. project/workspace structure; 2. framework configuration;
3. TypeScript configuration; 4. lint/format configuration; 5. content/truth model
foundation; 6. design-token foundation; 7. accessibility foundation; 8. SEO
foundation; 9. testing foundation; 10. deterministic validation commands;
11. CI/release checks; 12. minimal application shell.

Do NOT invent UK Bangla Tigers content. Use explicit placeholders where real
evidence is unavailable. **Every placeholder must be machine-distinguishable
from publishable content** — the truth gate has to be able to fail the build on
it, which means a naming or type convention, not a human eyeballing it.

After implementation, actually run: install · type check · lint · tests · build ·
truth/content validation · accessibility validation where applicable.

Record actual commands and actual exit codes. Do not fabricate PASS results.

Create `artifacts/receipts/FOUNDATION.md` conforming to `schemas/receipt.schema.json`.

Do not proceed into visual or content production.

End with:

```
FOUNDATION_STATUS = PASS | FAIL
TESTS = ...
BUILD = ...
CONTENT_TRUTH = ...
CHANGES = ...
```
